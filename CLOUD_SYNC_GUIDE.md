# 微信小程序云同步功能使用指南

## 功能简介

小程序现已支持多设备云同步功能，地址数据会自动同步到微信云开发，实现：
- ☁️ **多设备同步**：同一账号在不同设备上数据保持一致
- 📱 **离线可用**：本地缓存，无网络也能使用
- 🔄 **自动同步**：增删改操作自动同步到云端
- 💾 **数据安全**：云端备份，不怕数据丢失

## 开通步骤

### 1. 开通云开发环境

#### 在微信开发者工具中操作：

1. **打开云开发控制台**
   - 点击工具栏"云开发"按钮
   - 或者点击右上角"云开发" → "开通"

2. **创建云环境**
   - 环境名称：自定义（如：prod）
   - 选择基础版（免费）
   - 点击"创建"

3. **等待环境初始化**
   - 大约需要2-3分钟
   - 显示"开通成功"即可

#### 免费配额说明：
- 数据库：2GB 存储
- 云函数：1000万次调用/月
- 云存储：5GB 存储
- 对于个人小程序完全够用

### 2. 创建数据库集合

1. **进入数据库管理**
   - 云开发控制台 → 数据库
   - 点击"添加集合"

2. **创建集合**
   - 集合名称：`addresses`
   - 权限设置：仅创建者可读写（推荐）
   - 点击"确定"

3. **创建索引（可选，优化性能）**
   ```json
   {
     "index_name": "updateTime_desc",
     "index_fields": [
       {
         "field_name": "updateTime",
         "order": "desc"
       }
     ]
   }
   ```

### 3. 配置小程序

#### 修改 app.js 添加环境ID（可选）：

```javascript
wx.cloud.init({
  env: 'your-env-id', // 替换为你的环境ID
  traceUser: true
})
```

**如何获取环境ID：**
- 云开发控制台 → 设置 → 环境ID
- 格式类似：`cloud1-xxx`

**注意**：如果不填写 env，会使用默认环境

### 4. 设置权限

在云开发控制台 → 数据库 → `addresses` 集合 → 权限设置：

推荐权限配置：
```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

这样每个用户只能访问自己的数据。

## 使用说明

### 自动同步

开通云开发后，所有操作会自动同步：

- ✅ **添加地址** → 自动上传到云端
- ✅ **编辑地址** → 自动更新云端数据
- ✅ **删除地址** → 自动从云端删除
- ✅ **设置默认** → 自动同步默认状态

### 多设备同步

**场景1：两台手机**
1. 手机A添加地址
2. 手机B打开小程序 → 自动从云端加载
3. 两台手机数据一致

**场景2：更换手机**
1. 旧手机有10个地址
2. 新手机登录小程序 → 自动恢复所有地址
3. 数据完整迁移

### 离线模式

**网络断开时：**
- 可以正常增删改查
- 数据保存在本地
- 恢复网络后自动同步

**原理：**
- 优先使用云端数据
- 云端失败降级为本地模式
- 双重保障，体验流畅

## 测试步骤

### 测试1：基础同步

1. 设备A添加一个地址
2. 设备B打开小程序
3. 验证：设备B能看到新地址

### 测试2：编辑同步

1. 设备A编辑地址
2. 设备B刷新页面
3. 验证：设备B看到修改后的地址

### 测试3：删除同步

1. 设备A删除地址
2. 设备B刷新页面
3. 验证：设备B地址也被删除

### 测试4：离线模式

1. 关闭手机网络
2. 添加/编辑地址
3. 验证：操作正常
4. 恢复网络，刷新页面
5. 验证：云端已更新（目前本地优先）

## 数据查看

### 在云开发控制台查看数据：

1. 打开：云开发 → 数据库 → addresses
2. 可以看到所有地址记录
3. 每条记录包含：
   - `_id`：云端ID
   - `_openid`：用户ID（自动添加）
   - `name`：收货人
   - `phone`：电话
   - `province/city/district`：地区
   - `detail`：详细地址
   - `tag`：标签
   - `isDefault`：是否默认
   - `createTime/updateTime`：时间戳

## 常见问题

### Q1: 云开发控制台打不开？

**A:**
- 检查小程序 AppID 是否正确
- 确认不是游客模式
- 重启微信开发者工具

### Q2: 提示"云开发未初始化"？

**A:**
- 检查基础库版本 ≥ 2.2.3
- 确认 app.js 中已调用 `wx.cloud.init()`
- 查看控制台是否有错误

### Q3: 数据不同步？

**A:**
- 检查网络连接
- 确认已创建 addresses 集合
- 查看控制台错误日志
- 检查数据库权限设置

### Q4: 显示"权限不足"？

**A:**
- 进入数据库权限设置
- 改为"仅创建者可读写"
- 或使用自定义权限

### Q5: 不同账号数据互通了？

**A:**
- 检查数据库权限配置
- 确保权限规则包含 `_openid` 验证
- 重新设置权限

### Q6: 如何清空云端数据？

**A:**
- 云开发控制台 → 数据库 → addresses
- 选中记录 → 批量删除
- 或者删除整个集合重新创建

## 关闭云同步

如果不想使用云同步：

1. **方法1：不开通云开发**
   - 小程序会自动使用纯本地模式
   - 不影响任何功能

2. **方法2：注释云开发初始化**
   ```javascript
   // app.js
   // wx.cloud.init({ ... })
   ```

3. **方法3：强制本地模式**
   ```javascript
   // 修改 address-storage.js
   // cloudStorage.isCloudEnabled()
   // 改为 false
   ```

## 数据迁移

### 从本地迁移到云端

如果之前有本地数据：

1. 开通云开发
2. 打开小程序
3. 本地数据会自动上传云端
4. 完成后云端和本地数据一致

### 从云端清空数据

```javascript
// 在小程序中执行（临时代码）
addressStorage.clearAllAddress()
```

## 费用说明

### 免费额度（完全够用）

- 存储：假设每个地址 1KB
- 100个地址 = 100KB
- 10000个地址 = 10MB
- 远远低于2GB限制

- 读写：每次操作1次请求
- 每天100次操作 = 3000次/月
- 远远低于1000万次限制

### 超出免费额度

个人小程序基本不会超出，除非：
- 地址数量超过100万
- 每天操作超过30万次

## 技术架构

```
┌─────────────┐
│   微信小程序  │
└──────┬──────┘
       │
       ├─ 本地存储 (localStorage)
       │  └─ 离线模式
       │
       ├─ 云开发 (wx.cloud)
       │  ├─ 云数据库 (addresses)
       │  └─ 自动同步
       │
       └─ 混合模式
          ├─ 优先云端
          ├─ 失败降级
          └─ 双重保障
```

## 下一步优化

可以继续增强：

1. **冲突解决**
   - 检测数据冲突
   - 提供合并策略

2. **同步状态显示**
   - 显示"已同步"图标
   - 显示最后同步时间

3. **手动同步**
   - 下拉刷新同步
   - 同步按钮

4. **数据导出**
   - 导出为 JSON
   - 导出为 Excel

---

## 总结

✅ **已实现功能：**
- 云端存储
- 自动同步
- 多设备访问
- 离线可用
- 数据安全

🎯 **使用建议：**
- 个人开发：直接使用免费版
- 上线发布：开通正式环境
- 数据隔离：配置好权限

📞 **需要帮助？**
- 查看微信云开发文档
- 查看小程序控制台日志
- 检查网络和权限配置

祝你的小程序云同步功能运行顺利！☁️
